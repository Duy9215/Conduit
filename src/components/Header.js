import React from 'react';
import Navbar from './Navbar';
import { useState } from 'react';

const Header = () => {
    return (
        <div>
            <Navbar />
        </div>
    );
};

export default Header;